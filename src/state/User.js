import { Map } from 'immutable';

export default Map({
    logout: Map({
        users: '',
        password:''
    }),
    groupData:Map(
        {
            name:'',
            caracteristicas:''
        }
    )
});
